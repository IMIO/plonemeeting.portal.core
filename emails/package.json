{
  "name": "plonemeeting-portal-core-emails",
  "private": true,
  "description": "Maizzle project for plonemeeting.portal.core's own transactional mails. Dev-only: pruned from the sdist, never installed in production.",
  "scripts": {
    "build": "maizzle build",
    "postinstall": "maizzle prepare"
  },
  "dependencies": {
    "@maizzle/framework": ">=6.0.5"
  }
}
